#include <iostream>
using namespace std;

int main() {
   cin.tie(NULL)->sync_with_stdio(false);

   int N, d;
   cin >> N >> d;
   cout << "YES" << '\n';
   cout << d * 1111;
   
   return 0;
}